// Code generated by SQLBoiler 4.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// BuildLog is an object representing the database table.
type BuildLog struct {
	ID         string    `boil:"id" json:"id" toml:"id" yaml:"id"`
	Result     string    `boil:"result" json:"result" toml:"result" yaml:"result"`
	StartedAt  time.Time `boil:"started_at" json:"started_at" toml:"started_at" yaml:"started_at"`
	FinishedAt null.Time `boil:"finished_at" json:"finished_at,omitempty" toml:"finished_at" yaml:"finished_at,omitempty"`
	BranchID   string    `boil:"branch_id" json:"branch_id" toml:"branch_id" yaml:"branch_id"`

	R *buildLogR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L buildLogL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var BuildLogColumns = struct {
	ID         string
	Result     string
	StartedAt  string
	FinishedAt string
	BranchID   string
}{
	ID:         "id",
	Result:     "result",
	StartedAt:  "started_at",
	FinishedAt: "finished_at",
	BranchID:   "branch_id",
}

// Generated where

var BuildLogWhere = struct {
	ID         whereHelperstring
	Result     whereHelperstring
	StartedAt  whereHelpertime_Time
	FinishedAt whereHelpernull_Time
	BranchID   whereHelperstring
}{
	ID:         whereHelperstring{field: "`build_logs`.`id`"},
	Result:     whereHelperstring{field: "`build_logs`.`result`"},
	StartedAt:  whereHelpertime_Time{field: "`build_logs`.`started_at`"},
	FinishedAt: whereHelpernull_Time{field: "`build_logs`.`finished_at`"},
	BranchID:   whereHelperstring{field: "`build_logs`.`branch_id`"},
}

// BuildLogRels is where relationship names are stored.
var BuildLogRels = struct {
	Branch        string
	Artifact      string
	BuildBranches string
}{
	Branch:        "Branch",
	Artifact:      "Artifact",
	BuildBranches: "BuildBranches",
}

// buildLogR is where relationships are stored.
type buildLogR struct {
	Branch        *Branch     `boil:"Branch" json:"Branch" toml:"Branch" yaml:"Branch"`
	Artifact      *Artifact   `boil:"Artifact" json:"Artifact" toml:"Artifact" yaml:"Artifact"`
	BuildBranches BranchSlice `boil:"BuildBranches" json:"BuildBranches" toml:"BuildBranches" yaml:"BuildBranches"`
}

// NewStruct creates a new relationship struct
func (*buildLogR) NewStruct() *buildLogR {
	return &buildLogR{}
}

// buildLogL is where Load methods for each relationship are stored.
type buildLogL struct{}

var (
	buildLogAllColumns            = []string{"id", "result", "started_at", "finished_at", "branch_id"}
	buildLogColumnsWithoutDefault = []string{"id", "result", "started_at", "finished_at", "branch_id"}
	buildLogColumnsWithDefault    = []string{}
	buildLogPrimaryKeyColumns     = []string{"id"}
)

type (
	// BuildLogSlice is an alias for a slice of pointers to BuildLog.
	// This should generally be used opposed to []BuildLog.
	BuildLogSlice []*BuildLog
	// BuildLogHook is the signature for custom BuildLog hook methods
	BuildLogHook func(context.Context, boil.ContextExecutor, *BuildLog) error

	buildLogQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	buildLogType                 = reflect.TypeOf(&BuildLog{})
	buildLogMapping              = queries.MakeStructMapping(buildLogType)
	buildLogPrimaryKeyMapping, _ = queries.BindMapping(buildLogType, buildLogMapping, buildLogPrimaryKeyColumns)
	buildLogInsertCacheMut       sync.RWMutex
	buildLogInsertCache          = make(map[string]insertCache)
	buildLogUpdateCacheMut       sync.RWMutex
	buildLogUpdateCache          = make(map[string]updateCache)
	buildLogUpsertCacheMut       sync.RWMutex
	buildLogUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var buildLogBeforeInsertHooks []BuildLogHook
var buildLogBeforeUpdateHooks []BuildLogHook
var buildLogBeforeDeleteHooks []BuildLogHook
var buildLogBeforeUpsertHooks []BuildLogHook

var buildLogAfterInsertHooks []BuildLogHook
var buildLogAfterSelectHooks []BuildLogHook
var buildLogAfterUpdateHooks []BuildLogHook
var buildLogAfterDeleteHooks []BuildLogHook
var buildLogAfterUpsertHooks []BuildLogHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *BuildLog) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range buildLogBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *BuildLog) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range buildLogBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *BuildLog) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range buildLogBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *BuildLog) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range buildLogBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *BuildLog) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range buildLogAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *BuildLog) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range buildLogAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *BuildLog) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range buildLogAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *BuildLog) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range buildLogAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *BuildLog) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range buildLogAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddBuildLogHook registers your hook function for all future operations.
func AddBuildLogHook(hookPoint boil.HookPoint, buildLogHook BuildLogHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		buildLogBeforeInsertHooks = append(buildLogBeforeInsertHooks, buildLogHook)
	case boil.BeforeUpdateHook:
		buildLogBeforeUpdateHooks = append(buildLogBeforeUpdateHooks, buildLogHook)
	case boil.BeforeDeleteHook:
		buildLogBeforeDeleteHooks = append(buildLogBeforeDeleteHooks, buildLogHook)
	case boil.BeforeUpsertHook:
		buildLogBeforeUpsertHooks = append(buildLogBeforeUpsertHooks, buildLogHook)
	case boil.AfterInsertHook:
		buildLogAfterInsertHooks = append(buildLogAfterInsertHooks, buildLogHook)
	case boil.AfterSelectHook:
		buildLogAfterSelectHooks = append(buildLogAfterSelectHooks, buildLogHook)
	case boil.AfterUpdateHook:
		buildLogAfterUpdateHooks = append(buildLogAfterUpdateHooks, buildLogHook)
	case boil.AfterDeleteHook:
		buildLogAfterDeleteHooks = append(buildLogAfterDeleteHooks, buildLogHook)
	case boil.AfterUpsertHook:
		buildLogAfterUpsertHooks = append(buildLogAfterUpsertHooks, buildLogHook)
	}
}

// One returns a single buildLog record from the query.
func (q buildLogQuery) One(ctx context.Context, exec boil.ContextExecutor) (*BuildLog, error) {
	o := &BuildLog{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for build_logs")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all BuildLog records from the query.
func (q buildLogQuery) All(ctx context.Context, exec boil.ContextExecutor) (BuildLogSlice, error) {
	var o []*BuildLog

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to BuildLog slice")
	}

	if len(buildLogAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all BuildLog records in the query.
func (q buildLogQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count build_logs rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q buildLogQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if build_logs exists")
	}

	return count > 0, nil
}

// Branch pointed to by the foreign key.
func (o *BuildLog) Branch(mods ...qm.QueryMod) branchQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`id` = ?", o.BranchID),
	}

	queryMods = append(queryMods, mods...)

	query := Branches(queryMods...)
	queries.SetFrom(query.Query, "`branches`")

	return query
}

// Artifact pointed to by the foreign key.
func (o *BuildLog) Artifact(mods ...qm.QueryMod) artifactQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`build_log_id` = ?", o.ID),
	}

	queryMods = append(queryMods, mods...)

	query := Artifacts(queryMods...)
	queries.SetFrom(query.Query, "`artifacts`")

	return query
}

// BuildBranches retrieves all the branch's Branches with an executor via build_id column.
func (o *BuildLog) BuildBranches(mods ...qm.QueryMod) branchQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`branches`.`build_id`=?", o.ID),
	)

	query := Branches(queryMods...)
	queries.SetFrom(query.Query, "`branches`")

	if len(queries.GetSelect(query.Query)) == 0 {
		queries.SetSelect(query.Query, []string{"`branches`.*"})
	}

	return query
}

// LoadBranch allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (buildLogL) LoadBranch(ctx context.Context, e boil.ContextExecutor, singular bool, maybeBuildLog interface{}, mods queries.Applicator) error {
	var slice []*BuildLog
	var object *BuildLog

	if singular {
		object = maybeBuildLog.(*BuildLog)
	} else {
		slice = *maybeBuildLog.(*[]*BuildLog)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &buildLogR{}
		}
		args = append(args, object.BranchID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &buildLogR{}
			}

			for _, a := range args {
				if a == obj.BranchID {
					continue Outer
				}
			}

			args = append(args, obj.BranchID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`branches`),
		qm.WhereIn(`branches.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Branch")
	}

	var resultSlice []*Branch
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Branch")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for branches")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for branches")
	}

	if len(buildLogAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Branch = foreign
		if foreign.R == nil {
			foreign.R = &branchR{}
		}
		foreign.R.BuildLogs = append(foreign.R.BuildLogs, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.BranchID == foreign.ID {
				local.R.Branch = foreign
				if foreign.R == nil {
					foreign.R = &branchR{}
				}
				foreign.R.BuildLogs = append(foreign.R.BuildLogs, local)
				break
			}
		}
	}

	return nil
}

// LoadArtifact allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-1 relationship.
func (buildLogL) LoadArtifact(ctx context.Context, e boil.ContextExecutor, singular bool, maybeBuildLog interface{}, mods queries.Applicator) error {
	var slice []*BuildLog
	var object *BuildLog

	if singular {
		object = maybeBuildLog.(*BuildLog)
	} else {
		slice = *maybeBuildLog.(*[]*BuildLog)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &buildLogR{}
		}
		args = append(args, object.ID)
	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &buildLogR{}
			}

			for _, a := range args {
				if a == obj.ID {
					continue Outer
				}
			}

			args = append(args, obj.ID)
		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`artifacts`),
		qm.WhereIn(`artifacts.build_log_id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Artifact")
	}

	var resultSlice []*Artifact
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Artifact")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for artifacts")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for artifacts")
	}

	if len(buildLogAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Artifact = foreign
		if foreign.R == nil {
			foreign.R = &artifactR{}
		}
		foreign.R.BuildLog = object
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ID == foreign.BuildLogID {
				local.R.Artifact = foreign
				if foreign.R == nil {
					foreign.R = &artifactR{}
				}
				foreign.R.BuildLog = local
				break
			}
		}
	}

	return nil
}

// LoadBuildBranches allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (buildLogL) LoadBuildBranches(ctx context.Context, e boil.ContextExecutor, singular bool, maybeBuildLog interface{}, mods queries.Applicator) error {
	var slice []*BuildLog
	var object *BuildLog

	if singular {
		object = maybeBuildLog.(*BuildLog)
	} else {
		slice = *maybeBuildLog.(*[]*BuildLog)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &buildLogR{}
		}
		args = append(args, object.ID)
	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &buildLogR{}
			}

			for _, a := range args {
				if queries.Equal(a, obj.ID) {
					continue Outer
				}
			}

			args = append(args, obj.ID)
		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`branches`),
		qm.WhereIn(`branches.build_id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load branches")
	}

	var resultSlice []*Branch
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice branches")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on branches")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for branches")
	}

	if len(branchAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.BuildBranches = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &branchR{}
			}
			foreign.R.Build = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.ID, foreign.BuildID) {
				local.R.BuildBranches = append(local.R.BuildBranches, foreign)
				if foreign.R == nil {
					foreign.R = &branchR{}
				}
				foreign.R.Build = local
				break
			}
		}
	}

	return nil
}

// SetBranch of the buildLog to the related item.
// Sets o.R.Branch to related.
// Adds o to related.R.BuildLogs.
func (o *BuildLog) SetBranch(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Branch) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `build_logs` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"branch_id"}),
		strmangle.WhereClause("`", "`", 0, buildLogPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.BranchID = related.ID
	if o.R == nil {
		o.R = &buildLogR{
			Branch: related,
		}
	} else {
		o.R.Branch = related
	}

	if related.R == nil {
		related.R = &branchR{
			BuildLogs: BuildLogSlice{o},
		}
	} else {
		related.R.BuildLogs = append(related.R.BuildLogs, o)
	}

	return nil
}

// SetArtifact of the buildLog to the related item.
// Sets o.R.Artifact to related.
// Adds o to related.R.BuildLog.
func (o *BuildLog) SetArtifact(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Artifact) error {
	var err error

	if insert {
		related.BuildLogID = o.ID

		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	} else {
		updateQuery := fmt.Sprintf(
			"UPDATE `artifacts` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, []string{"build_log_id"}),
			strmangle.WhereClause("`", "`", 0, artifactPrimaryKeyColumns),
		)
		values := []interface{}{o.ID, related.ID}

		if boil.IsDebug(ctx) {
			writer := boil.DebugWriterFrom(ctx)
			fmt.Fprintln(writer, updateQuery)
			fmt.Fprintln(writer, values)
		}
		if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
			return errors.Wrap(err, "failed to update foreign table")
		}

		related.BuildLogID = o.ID

	}

	if o.R == nil {
		o.R = &buildLogR{
			Artifact: related,
		}
	} else {
		o.R.Artifact = related
	}

	if related.R == nil {
		related.R = &artifactR{
			BuildLog: o,
		}
	} else {
		related.R.BuildLog = o
	}
	return nil
}

// AddBuildBranches adds the given related objects to the existing relationships
// of the build_log, optionally inserting them as new records.
// Appends related to o.R.BuildBranches.
// Sets related.R.Build appropriately.
func (o *BuildLog) AddBuildBranches(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Branch) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.BuildID, o.ID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `branches` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"build_id"}),
				strmangle.WhereClause("`", "`", 0, branchPrimaryKeyColumns),
			)
			values := []interface{}{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.BuildID, o.ID)
		}
	}

	if o.R == nil {
		o.R = &buildLogR{
			BuildBranches: related,
		}
	} else {
		o.R.BuildBranches = append(o.R.BuildBranches, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &branchR{
				Build: o,
			}
		} else {
			rel.R.Build = o
		}
	}
	return nil
}

// SetBuildBranches removes all previously related items of the
// build_log replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Build's BuildBranches accordingly.
// Replaces o.R.BuildBranches with related.
// Sets related.R.Build's BuildBranches accordingly.
func (o *BuildLog) SetBuildBranches(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Branch) error {
	query := "update `branches` set `build_id` = null where `build_id` = ?"
	values := []interface{}{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.BuildBranches {
			queries.SetScanner(&rel.BuildID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Build = nil
		}

		o.R.BuildBranches = nil
	}
	return o.AddBuildBranches(ctx, exec, insert, related...)
}

// RemoveBuildBranches relationships from objects passed in.
// Removes related items from R.BuildBranches (uses pointer comparison, removal does not keep order)
// Sets related.R.Build.
func (o *BuildLog) RemoveBuildBranches(ctx context.Context, exec boil.ContextExecutor, related ...*Branch) error {
	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.BuildID, nil)
		if rel.R != nil {
			rel.R.Build = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("build_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.BuildBranches {
			if rel != ri {
				continue
			}

			ln := len(o.R.BuildBranches)
			if ln > 1 && i < ln-1 {
				o.R.BuildBranches[i] = o.R.BuildBranches[ln-1]
			}
			o.R.BuildBranches = o.R.BuildBranches[:ln-1]
			break
		}
	}

	return nil
}

// BuildLogs retrieves all the records using an executor.
func BuildLogs(mods ...qm.QueryMod) buildLogQuery {
	mods = append(mods, qm.From("`build_logs`"))
	return buildLogQuery{NewQuery(mods...)}
}

// FindBuildLog retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindBuildLog(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*BuildLog, error) {
	buildLogObj := &BuildLog{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `build_logs` where `id`=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, buildLogObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from build_logs")
	}

	return buildLogObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *BuildLog) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no build_logs provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(buildLogColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	buildLogInsertCacheMut.RLock()
	cache, cached := buildLogInsertCache[key]
	buildLogInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			buildLogAllColumns,
			buildLogColumnsWithDefault,
			buildLogColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(buildLogType, buildLogMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(buildLogType, buildLogMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `build_logs` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `build_logs` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `build_logs` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, buildLogPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into build_logs")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for build_logs")
	}

CacheNoHooks:
	if !cached {
		buildLogInsertCacheMut.Lock()
		buildLogInsertCache[key] = cache
		buildLogInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the BuildLog.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *BuildLog) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	buildLogUpdateCacheMut.RLock()
	cache, cached := buildLogUpdateCache[key]
	buildLogUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			buildLogAllColumns,
			buildLogPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update build_logs, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `build_logs` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, buildLogPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(buildLogType, buildLogMapping, append(wl, buildLogPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update build_logs row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for build_logs")
	}

	if !cached {
		buildLogUpdateCacheMut.Lock()
		buildLogUpdateCache[key] = cache
		buildLogUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q buildLogQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for build_logs")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for build_logs")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o BuildLogSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), buildLogPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `build_logs` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, buildLogPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in buildLog slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all buildLog")
	}
	return rowsAff, nil
}

var mySQLBuildLogUniqueColumns = []string{
	"id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *BuildLog) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no build_logs provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(buildLogColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLBuildLogUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	buildLogUpsertCacheMut.RLock()
	cache, cached := buildLogUpsertCache[key]
	buildLogUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			buildLogAllColumns,
			buildLogColumnsWithDefault,
			buildLogColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			buildLogAllColumns,
			buildLogPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert build_logs, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`build_logs`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `build_logs` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(buildLogType, buildLogMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(buildLogType, buildLogMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for build_logs")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(buildLogType, buildLogMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for build_logs")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for build_logs")
	}

CacheNoHooks:
	if !cached {
		buildLogUpsertCacheMut.Lock()
		buildLogUpsertCache[key] = cache
		buildLogUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single BuildLog record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *BuildLog) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no BuildLog provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), buildLogPrimaryKeyMapping)
	sql := "DELETE FROM `build_logs` WHERE `id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from build_logs")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for build_logs")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q buildLogQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no buildLogQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from build_logs")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for build_logs")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o BuildLogSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(buildLogBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), buildLogPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `build_logs` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, buildLogPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from buildLog slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for build_logs")
	}

	if len(buildLogAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *BuildLog) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindBuildLog(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *BuildLogSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := BuildLogSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), buildLogPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `build_logs`.* FROM `build_logs` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, buildLogPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in BuildLogSlice")
	}

	*o = slice

	return nil
}

// BuildLogExists checks if the BuildLog row exists.
func BuildLogExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `build_logs` where `id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if build_logs exists")
	}

	return exists, nil
}
