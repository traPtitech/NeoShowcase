// Code generated by Wire. DO NOT EDIT.

//go:generate go run github.com/google/wire/cmd/wire
//go:build !wireinject
// +build !wireinject

package main

import (
	"github.com/traPtitech/neoshowcase/pkg/infrastructure/repository"
	"github.com/traPtitech/neoshowcase/pkg/usecase/gitea-integration"
)

// Injectors from wire.go:

func NewServer(c Config) (*Server, error) {
	config := c.Gitea
	repositoryConfig := c.DB
	db, err := repository.New(repositoryConfig)
	if err != nil {
		return nil, err
	}
	gitRepositoryRepository := repository.NewGitRepositoryRepository(db)
	userRepository := repository.NewUserRepository(db)
	integration, err := giteaintegration.NewIntegration(config, gitRepositoryRepository, userRepository)
	if err != nil {
		return nil, err
	}
	server := &Server{
		integration: integration,
		db:          db,
	}
	return server, nil
}
