apiVersion: v1
kind: Service
metadata:
  name: {{ $.Release.Name }}-gateway
  namespace: {{ $.Release.Namespace }}

spec:
  type: ClusterIP
  selector:
    app: {{ $.Release.Name }}-gateway
  ports:
    - name: http
      port: 80
      targetPort: http
