name: Release helm chart

on:
  push:
    tags:
      - helm/v*.*.*
    branches-ignore:
      - '**'

  workflow_call:
    inputs:
      ref_name:
        description: Git ref to checkout (tag or branch)
        type: string
        required: true

env:
  REGISTRY_USER: traptitech
  CHART: neoshowcase

jobs:
  release-helm:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ inputs.ref_name || github.ref_name }}

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ env.REGISTRY_USER }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish chart
        run: |
          cd charts
          ./publish.sh ${{ env.CHART }} oci://ghcr.io/${{ env.REGISTRY_USER }}/charts

  dispatch-renovate:
    name: Dispatch Renovate
    runs-on: ubuntu-latest
    needs:
      - release-helm
    steps:
      - uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.DISPATCH_PAT }}
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: 'traPtitech',
              repo: 'manifest',
              workflow_id: 'renovate.yaml',
              ref: 'main'
            })
